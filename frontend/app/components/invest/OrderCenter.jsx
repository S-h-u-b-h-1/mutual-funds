"use client";

import { useEffect, useState } from "react";
import InvestShell, { Card, StatusPill } from "./InvestShell";
import TransactionTimeline from "./TransactionTimeline";
import SchemePicker from "./SchemePicker";
import { investApi } from "../../lib/invest/api";
import { ORDER_LABELS as labels, ORDER_STAGES as stageOrder, ORDER_STATUS_META as statusMeta } from "../../lib/invest/transactionStatus";

const money = value => value == null ? "Not available" : new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(Number(value));

export default function OrderCenter() {
  const [orders, setOrders] = useState([]); const [loading, setLoading] = useState(true); const [apiReady, setApiReady] = useState(null); const [error, setError] = useState(""); const [review, setReview] = useState(false); const [saving, setSaving] = useState(false); const [draftOrder, setDraftOrder] = useState(null); const [confirmation, setConfirmation] = useState(null); const [detail, setDetail] = useState(null); const [scheme, setScheme] = useState(null); const [amount, setAmount] = useState("");
  async function load() { try { const value = await investApi.getOrders(); setOrders(value.orders || []); setApiReady(true); setError(""); } catch (err) { setApiReady(false); setError(err.message); } finally { setLoading(false); } }
  useEffect(() => { load(); }, []);
  const active = orders.some(order => ["submitted", "processing", "units_pending"].includes(order.status));
  useEffect(() => { if (!active) return undefined; const timer = setInterval(load, 6000); return () => clearInterval(timer); }, [active]);
  async function prepareReview(event) { event.preventDefault(); if (!scheme?.code) { setError("Choose a scheme from the search results before continuing."); return; } setSaving(true); setError(""); try { const value = await investApi.createOrder({ schemeCode: scheme.code, orderType: "purchase", amount: Number(amount), draft: true }); setDraftOrder(value.order); setReview(true); await load(); } catch (err) { setError(err.message); } finally { setSaving(false); } }
  async function submit() { setSaving(true); setError(""); try { const value = await investApi.submitOrder(draftOrder.id); setConfirmation(value.order); setDraftOrder(null); setReview(false); setScheme(null); setAmount(""); await load(); } catch (err) { setError(err.message); } finally { setSaving(false); } }
  async function cancelDraft() { setSaving(true); setError(""); try { await investApi.cancelOrder(draftOrder.id); setDraftOrder(null); setReview(false); } catch (err) { setError(err.message); } finally { setSaving(false); } }
  async function openDetails(order) { setError(""); try { setDetail(await investApi.getOrder(order.id)); } catch (err) { setError(err.message); } }
  async function mutate(orderId, action) { setSaving(true); setError(""); try { const value = await action(orderId); setOrders(current => current.map(order => order.id === orderId ? value.order : order)); } catch (err) { setError(err.message); } finally { setSaving(false); } }
  return <InvestShell title="Every order, explained." description="Choose a fund by name, review the server-saved draft, and follow every backend status change." actions={<a href="/funds" className="inline-flex min-h-11 items-center rounded-full border border-line bg-surface px-5 text-sm font-semibold text-ink">Research funds</a>}>
    <div className="mb-5 rounded-2xl border border-information/20 bg-information/10 p-4 text-xs leading-5 text-ink-muted"><strong className="text-information">Provider status.</strong> Payment outcome and provider metadata appear only when returned by the backend.</div>
    {confirmation && <><TransactionTimeline currentStatus={confirmation.status} reference={confirmation.id} amount={confirmation.amount} scheme={confirmation.scheme_code} nextStep={confirmation.status === "submitted" ? "Provider processing update" : "Backend lifecycle update"} /><p className="mt-3 rounded-2xl bg-surface-2 p-4 text-xs leading-5 text-ink-muted">Payment: <strong className="text-ink">{confirmation.payment_status || "Not supplied"}</strong>{confirmation.payment_reference && <> · Reference <strong className="text-ink">{confirmation.payment_reference}</strong></>}</p></>}
    <div className="grid gap-5 xl:grid-cols-[390px_minmax(0,1fr)]"><Card className="h-fit p-5 sm:p-6"><div className="text-[10px] font-bold uppercase tracking-[.16em] text-accent">New purchase</div><h2 className="mt-2 text-xl font-semibold text-ink">{review ? "Review before submitting" : "Investment details"}</h2>{review ? <div className="mt-5"><div className="rounded-2xl border border-accent/20 bg-accent/5 p-4 text-sm text-ink">Draft saved on the server.</div><dl className="mt-4 grid gap-3 rounded-2xl bg-surface-2 p-4 text-sm"><div className="flex justify-between gap-4"><dt className="text-ink-faint">Scheme</dt><dd className="text-right font-semibold text-ink">{scheme?.name || "Scheme name unavailable"}<span className="block text-xs font-normal text-ink-faint">{scheme?.plan || "Plan unavailable"} · {scheme?.option || "Option unavailable"}</span></dd></div><div className="flex justify-between gap-4"><dt className="text-ink-faint">Amount</dt><dd className="font-semibold text-ink">{money(amount)}</dd></div></dl><div className="mt-5 flex gap-2"><button type="button" onClick={() => setReview(false)} disabled={saving} className="min-h-11 flex-1 rounded-full border border-line text-sm font-semibold text-ink">Edit</button><button type="button" onClick={submit} disabled={saving} className="min-h-11 flex-1 rounded-full bg-accent text-sm font-semibold text-white">{saving ? "Submitting…" : "Submit purchase"}</button></div></div> : <form onSubmit={prepareReview} className="mt-5 grid gap-5"><SchemePicker value={scheme} onChange={setScheme} label="Fund or scheme" required /><label className="text-xs font-semibold text-ink-muted">Amount in rupees<input className="mt-2 min-h-12 w-full rounded-2xl border border-line bg-bg px-4 text-sm text-ink outline-none focus:border-accent focus:ring-2 focus:ring-accent/15" type="number" min="1" value={amount} onChange={event => setAmount(event.target.value)} required /></label><button type="submit" disabled={!apiReady || saving || !scheme} className="min-h-12 rounded-full bg-ink px-5 text-sm font-semibold text-bg disabled:opacity-40">{saving ? "Saving draft…" : apiReady === null ? "Checking order service…" : apiReady ? "Review purchase" : "Order service unavailable"}</button></form>}{error && <p role="alert" className="mt-4 rounded-2xl bg-neg/10 p-4 text-xs leading-5 text-neg">{error}</p>}</Card>
      <div className="grid content-start gap-4"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold text-ink">Order history</h2><button type="button" onClick={load} className="min-h-10 rounded-full border border-line px-4 text-xs font-semibold text-ink">Refresh</button></div>{loading ? <div className="h-44 skeleton rounded-[1.6rem]" role="status" aria-label="Loading orders" aria-busy="true" /> : orders.length === 0 ? <Card className="grid min-h-64 place-items-center p-7 text-center"><div><div className="text-lg font-semibold text-ink">No investment orders yet</div><p className="mt-2 text-sm text-ink-muted">Your first purchase will appear after the server accepts it.</p></div></Card> : orders.map(order => { const meta = statusMeta[order.status] || [order.status, "The provider returned an unknown state."]; const current = Math.max(0, stageOrder.indexOf(order.status)); return <Card key={order.id} className="p-5"><button type="button" onClick={() => openDetails(order)} className="block w-full text-left"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="text-xs capitalize text-ink-faint">{order.order_type?.replaceAll("_", " ")}</div><div className="mt-1 text-lg font-semibold text-ink">{order.amount != null ? money(order.amount) : `${order.units} units`}</div><div className="mt-1 text-xs text-ink-faint">{order.plan || order.option ? `${order.plan || "Plan unavailable"} · ${order.option || "Option unavailable"}` : "Plan and option unavailable"}</div></div><StatusPill status={order.status} /></div><div className="mt-5 grid grid-cols-5 gap-1" aria-label={`Order status: ${order.status}`}>{stageOrder.map((stage, index) => <div key={stage} className="min-w-0"><div className={`h-1.5 rounded-full ${current >= index ? "bg-accent" : "bg-line"}`} /><div className="mt-2 hidden truncate text-[9px] text-ink-faint sm:block">{labels[stage]}</div></div>)}</div><div className="mt-4 rounded-2xl bg-surface-2 p-3"><div className="flex items-center justify-between gap-3"><span className="text-sm font-semibold text-ink">{meta[0]}</span><span className="text-xs text-accent">View timeline →</span></div><p className="mt-1 text-xs leading-5 text-ink-muted">{meta[1]}</p></div>{order.payment_status && <p className="mt-2 text-xs text-ink-faint">Payment: {order.payment_status}{order.provider_error_code ? ` · ${order.provider_error_code}` : ""}</p>}</button>{order.status === "retry_required" && <button type="button" onClick={() => mutate(order.id, investApi.retryOrder)} disabled={saving} className="mt-4 min-h-10 rounded-full bg-accent px-4 text-xs font-semibold text-white">Retry order</button>}{["draft", "submitted", "processing"].includes(order.status) && <button type="button" onClick={() => mutate(order.id, investApi.cancelOrder)} disabled={saving} className="mt-4 min-h-10 rounded-full border border-line px-4 text-xs font-semibold text-ink">Cancel</button>}</Card>; })}{detail && <TransactionTimeline currentStatus={detail.order?.status} reference={detail.order?.id} amount={detail.order?.amount} scheme={detail.order?.scheme_code} nextStep="Backend lifecycle update" events={detail.timeline || []} />}</div>
    </div>
  </InvestShell>;
}
