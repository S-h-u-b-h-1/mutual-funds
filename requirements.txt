# Core ingestion
psycopg[binary]==3.2.13
requests==2.32.3

# API
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Transform / quality (added in Phase 2)
dbt-postgres==1.9.0

# Dev
pytest==8.3.4

# caching
redis==5.2.1
openpyxl==3.1.5
xlrd==2.0.2
sentry-sdk==2.20.0

# factsheet ingestion (scripts/ingest_factsheets.py, ingestion/factsheet/) — real dependency,
# never previously listed here because nothing that imports it ran inside CI until Data Platform
# Mission 5's new test (test_hdfc_parse_merges_continuation_pages) became the first test in this
# suite to actually call HDFCAdapter.parse(pdf_bytes) instead of parse_scheme_block(text) — that
# exercises the `import pypdf` path base.py/hdfc.py/ingest_factsheets.py already depended on.
pypdf==6.14.2
